{
  "schemaVersion": 1,
  "id": "assurance-runtime-interface",
  "name": "Assurance Runtime Interface",
  "owner": "assurance-runtime",
  "consumers": ["project-model", "change-kernel", "context-kernel"],
  "maturity": "provisional",
  "normativeSources": ["product-intent", "assurance-policy"],
  "claims": [
    {
      "id": "repository-binding-is-content-exact",
      "statement": "A repository binding identifies HEAD, tracked differences, and content digests for every visible untracked file."
    },
    {
      "id": "repository-binding-fails-closed",
      "statement": "A failed or truncated Git observation is reported as unavailable and cannot become exact repository Evidence."
    },
    {
      "id": "tracked-path-observation-is-exact",
      "statement": "One bounded Git observation returns the complete canonical set of tracked repository-relative paths and its exact digest, or reports unavailable on failure, truncation, malformed paths, duplicates, or resource-limit exhaustion."
    },
    {
      "id": "repository-source-product-binds-stable-exact-bytes",
      "statement": "One opaque process-local Repository Source Product is issued only after two consecutive identical bounded composite observations bind the canonical repository identity and root, exact Git binding and tracked-path facts, one ordered exact concrete path set, and each regular file's byte length and content digest; its projection exposes no source bodies and every byte read requires the live product, so missing, extra, cross-repository, drifted, escaping, symlink, non-regular, unreadable, duplicate, malformed, forged, serialized, or over-limit input fails closed."
    },
    {
      "id": "repository-source-product-proof-rejects-binding-and-byte-attacks",
      "statement": "The fixed Repository Source Product proof accepts identical consecutive observations and A/B/B stabilization with reproducible manifest and byte projections, and rejects A/B/C instability, repository or Git-binding substitution, tracked-inventory drift, path-set mismatch, byte drift, manifest mismatch, traversal, symlink, non-regular file, unreadable file, duplicate path, forged or serialized product, unknown byte read, mutation aliasing, and resource-exhaustion attacks."
    },
    {
      "id": "command-observation-is-explicit",
      "statement": "A declared command returns explicit exit status, stdout, stderr, signal, and truncation state without deciding acceptance."
    },
    {
      "id": "command-observation-is-terminal-state-complete",
      "statement": "Under the declared local-direct-node-posix-v1 support profile, one command invocation returns one bounded schema-v2 observation that keeps support, control trigger, confirmed close, actual exit or signal, and each raw byte stream's capture, observed length, digest, strict UTF-8 validity, and truncation facts orthogonal."
    },
    {
      "id": "unsupported-command-observations-fail-closed",
      "statement": "Only a supported direct-argv observation with no control trigger, confirmed close, natural exit code zero, and internally consistent raw-byte stream facts can be classified as successful; unsupported profiles, malformed adapter reports, timeout, cancellation, start failure, signal termination, or unconfirmed close remain non-positive."
    }
  ]
}
